import { tv } from "tailwind-variants"

export const resultNodeStyles = tv({
  slots: {
    root: "nodrag nopan mt-2",
    fieldGroup: "space-y-1",
    label: "text-[11px] font-medium text-muted-foreground",
  },
})
