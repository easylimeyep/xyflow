## 1. OpenSpec Artifacts

- [x] 1.1 Create proposal, design, spec, and tasks artifacts for the large ELK canvas example.

## 2. Large Example Implementation

- [x] 2.1 Add a `LargeElkGraphExample` component that computes an initial graph through `createInitialGraphElk`.
- [x] 2.2 Define a valid graph input with about 40 nodes, a root Keyword start, true/false Result terminals, and one convergence node with at least 10 incoming edges.
- [x] 2.3 Add a separate examples-page tab for the large ELK graph while keeping the existing small ELK tab.

## 3. Verification

- [x] 3.1 Run focused typecheck/lint verification for the web app.
